import styled from 'styled-components';

const StyledSignUp = styled.form`
  & > .signup__input {
    display: block;
    margin-bottom: 30px;
  }

  & > .signup__btn {
    display: block;
    margin: 0 auto;
    margin-top: 80px;
  }
`;

export default StyledSignUp;
