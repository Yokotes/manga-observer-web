import styled from 'styled-components';

const StyledLogInModal = styled.form`
  & > .login__input {
    margin-bottom: 30px;
  }

  & > .login__btn {
    display: block;
    margin: 0 auto;
    margin-top: 50px;
  }
`;

export default StyledLogInModal;
